<mvc:View
	xmlns="sap.m"
	xmlns:form="sap.ui.layout.form"
	xmlns:l="sap.ui.layout"
	xmlns:mvc="sap.ui.core.mvc">

	<Panel headerText="{/panelHeaderText}" class="sapUiResponsiveMagrin" width="auto">
		<form:SimpleForm editable="true" layout="ColumnLayout">
			<Label text="{i18n>First Name}"/>
			<Input value='{/firstName}' valueLiveUpdate="true" width="200px" enabled="{/enabled}"/>
			<Label text="{i18n>Last Name}" />
			<Input value='{/lastName}' valueLiveUpdate="true" width="200px" enabled="{/enabled}"/>
			<Label text="{i18n>Enabled}" />
			<CheckBox selected="{/enabled}" />
		</form:SimpleForm>
	
	</Panel>
	<Panel headerText="{18m>panel2HeaderText}" class="sapUiResponsiveMargin" width="auto"> 
		<content>
			<l:VerticalLayout>
				<Label labelFor="address" text="{i18n>address}"/>
					<FormattedText class="sapUiSmallMarginBottom"
									htmlText="{/address/street}&lt;br>{/address/zip}
									{/adress/city}&lt;br>{/address/country}"
									id="address"
									width="200px"/>
					<Link href="{
						parts: [
						'/firstName',
						'/lastName'
						],
						formatter: '.formatMail'
					}"
					text="{i18n>sendEmail}"/>

			</l:VerticalLayout>

			<l:VerticalLayout >
				<Label labelFor="salesAmount" text="{i18n>salesAmount}:"></Label>
				<Input description="{/currencyCode}" enabled="{/enabled}" id="salesAmount"
						value="{ 
							parts: [
							{path: '/salesAmount'},
							{path: '/currencyCode'}
							],
							type: 'sap.ui.model.type.Currency',
							formatOptions: {showMeasure: false}
						 }"
						 width="200px"> </Input>
			</l:VerticalLayout>


		</content>
	</Panel>


</mvc:View>